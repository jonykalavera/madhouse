[core]
    autocrlf = input
    pager = delta

[user]
	email = sidecar@localhost
	name = Grafana Sidecar

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections
    side-by-side = true


    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true

[merge]
    tool = meld
    conflictstyle = diff3

[init]
	defaultBranch = main

[diff]
    tool = meld
    colorMoved = default

[difftool]
    prompt = false

[difftool "meld"]
    cmd = meld "$LOCAL" "$REMOTE"
[safe]
	directory = /home/jony/code/HouseOfTickets/monitoring-as-code/grafana/provisioning/dashboards/json
	directory = /home/jony/code/HouseOfTickets/monitoring-as-code/grafana/provisioning/dashboards/json
	directory = /tmp/test_dashboards_integration
[url "https://${GITHUB_TOKEN}@github.com/"]
	insteadOf = https://github.com/
